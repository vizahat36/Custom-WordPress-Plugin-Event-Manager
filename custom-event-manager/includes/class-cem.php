<?php
// Prevent direct access.
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

if ( ! class_exists( 'CEM_Plugin' ) ) {
    /**
     * Core plugin class placeholder for future expansion.
     */
    class CEM_Plugin {
        public function __construct() {
            // Hooks and logic will be added in subsequent steps.
        }
    }
}
